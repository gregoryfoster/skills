# Session 2026-08-20 — gregoryfoster/skills

Fourteen issues (#198, #200–212) → nine work items, three batches (4/4/1), one
deferral, tracking issue #213, design doc
`docs/plans/2026-08-20-skills-adoption-and-followup-backlog.md`.

## Interview answers

| Q | Answer |
|---|---|
| Q0 | #198+#209+#210 → **bundle** (one owning file, adoption-feedback pattern); #204+#205 → **bundle** (overlapping sweep surface); #203→#202 → **bundle, prerequisite first** (comment port); #207 → **defer** (upstream blocker) |
| Q1–Q4 | **Inherited from #199's doc, confirm-or-change** — `(F×2)+(C×3)+S` max 18 (third consecutive C×3), active production, nothing else deferred, hybrid, regular merge |
| Q5 | **Ceiling 4, host-bound. Fourth consecutive negative for this repo** — re-verified with one grep pass, per the inherit-the-ceiling clause |

Scores: #198/#208 = 14, #210 = 13, #202/#209/#200/#211/#201 = 11, #206 = 10,
#203/#205 = 8, #204/#212 = 7.

## A blocked prerequisite that is a PR, not an issue — and the release matters

#207's blocker is upstream giancarloerra/SocratiCode#112. The closed-prerequisite
rule says grep the finding, not the state; here the analogue was **check the PR
state AND the release list** — a merged PR without a release carrying it would
still defer, because the skill gates on installed-server behaviour. One
`gh pr view` + `gh release list` settled it in seconds. The deferral also shaped
a *survivor's* brief: #198 ships now but words its paragraph to point toward the
coming advisory, per #207's own cross-reference — a deferred issue can still
constrain the wording of a scheduled one.

## The consistency test that widened a one-line fix 4×

#211 named one variant; `test_pre_ship_env_override.py:76` asserts the env-block
is discovered in ALL_VARIANTS, and the snippet exists in four `pre-ship.sh`
files. The body's understatement was invisible to the issue's own file list —
found only by grepping the *snippet*, not the named file. Same lesson as the
bidirectional footprint grep, with a twist: **a block-consistency test turns any
single-variant fix into an all-variants fix by construction**, so grep for
consistency suites over the target before sizing a "one-line" issue.

## A section-boundary collision the section headers hid

#200 (install prose) and #210 (marker/rescue logic) both land inside
init-socraticode SKILL.md **Phase 3** — visible only by mapping the issues'
target lines onto the `^###` section spans (L135–199 contains L174–187). The
issues name different concerns and different artefacts; the heading map is what
showed one section owning both. That single overlap, plus the shared
`test_reminder_hook_vendored.py` (whose :120 sync test pins the prefetch string
in both doc and script), produced the plan's only cross-batch edge (A1 → B1).

## Decide-then-rescore: label arithmetic is part of the record

Three gate decisions (#206 backfill, #209 add-the-tools, #201 file knob). One
slip worth logging: the AskUserQuestion option labels carried wrong rescore
arithmetic (S1→2 labelled "9→12" where (F1×2)+(C2×3)+S2 = 10). The user chose on
the label. Corrected transparently in the next message before approval — but the
rule is: **compute the rescored total before writing it into an option label**;
a wrong number in the choice surface is a wrong number in the approval.

## Bundle-heavy Q0 kept the batch design trivial

Four Q0 dispositions absorbed every same-file pair before scoring: the three
socraticode-doc writers, the two curation judgements, and the comment-port
sequence. After that, conflict analysis found exactly one cross-batch edge and
zero intra-batch contested files — no line-window ownership needed anywhere.
Mixed provenance behaved as the geometry table predicts: followup-derived items
clustered on the #199 surfaces; adoption-feedback items clustered per owning
skill; the bundles *are* the clusters, one agent per owning file family.
