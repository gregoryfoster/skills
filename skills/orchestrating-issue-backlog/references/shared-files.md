# Shared files: the test surface, line windows, vacuous assertions

Loaded from [`SKILL.md`](../SKILL.md) Step 5 (item 2) whenever two issues touch the
same file — a test file above all, but the procedure carries any large shared file,
the policy file included. Step numbers refer to `SKILL.md`.

Demoted from the body in the #285 curation, as one paragraph: it is the whole of
Step 5 item 2.

**One exception to the window rule below, to check now rather than at Step 7:** a
token-gated file whose headroom is under one agent's plausible edit. In that file,
separated windows merge cleanly and still turn the gate red, so measure the
headroom of every gated file the backlog edits; where it is that tight, the
budget-tight rule in [`batch-design.md`](batch-design.md) makes it single-writer.
A gated file with room is an ordinary shared file, as the 2026-08-12 `AGENTS.md`
case below shows.

2. **Grep the test surface for the literal strings each fix rewrites.** A large shared test file is two conflict zones, and only one is visible from source-file overlap. The **append** half (every worker adding a test class at EOF) is solved by routing new tests to a new per-agent file. The **modify** half — existing assertions that a fix *invalidates* — is invisible to both the source grep and the issue bodies, which describe script changes and say nothing about tests. Grep the test file for the exact strings, paths and JSON keys each issue changes (process-log 2026-08-11 skills: a 3,560-line test file hardcoded the literal hook command one issue rewrote and the literal log path another changed; neither issue mentioned a test). Then **map each agent's owned line-window before assigning**: separated windows merge cleanly and the file stays contested without serializing the batch, but overlapping windows mean sequencing — which has to be decided here, not discovered at merge time. The technique is not test-specific — the governing property is whether the windows overlap, not the file's kind, so it carries any large shared file including the policy file itself (2026-08-12: three Batch A agents wrote `AGENTS.md` in three separated windows). A non-test file needs one extra clause: **no restructuring, additions within the window only** — a reorder merges cleanly and silently reshuffles another agent's window. In Markdown, own a **named section**, not a line range, and re-derive the range at launch: any edit above it moves it, so a range written at planning time is stale by the batch that consumes it (2026-08-17 watcher within a batch; 2026-08-17 skills across them — a window named L309–391 had become L306–388 three batches later). Name any genuinely shared line **read-only for both** rather than sequencing around it (2026-08-12: one `parametrize` list naming both contested scripts). When the file-level pass returns *everything touches everything*, escalate to function/region granularity before concluding the backlog is serial — a hub file is usually a set of independent regions (2026-08-13 cli: ten work items on one 525-line file ran 4-wide). Distinct from a shared *fixture* dependency, which is semantic and needs sequencing rather than a new file, and hides beyond `conftest.py` (2026-07-08): in module-local fixtures (2026-08-13 power-map), and in test fakes of a protocol the backlog changes, which region-disjoint agents all build on (2026-09-14 observo) — `grep -rl 'def <method>' tests/`. **Then grep for the assertions the fix makes *vacuous*, not only the ones it breaks** — the modify-half goes red and the suite says so; this half goes **green**. When a change moves *which* column carries a piece of state, an assertion comparing the old column to a snapshot of itself degrades to `None == None`: still passing, verifying nothing, and reachable by no keyword sweep, since it names neither the literal the fix removes nor the one it adds (2026-08-14 observo: two idempotency tests silently stopped checking the issue's own acceptance criterion). Re-anchor them on the new field *and* assert it non-null, so the replacement cannot degrade the same way. **A second mechanism reaches the same green: an assertion vacuous *as filed*, because the test never traverses the layer the defect lives in.** An issue body's "pin it in test X" is a claim like any other, so read X's *setup* rather than its assertions — a helper test that builds the framework's request object itself and calls the function under test directly bypasses the framework's own sanitizing layer, and the prescribed pin passes **before** the fix (2026-09-16 wordpress: `new WP_REST_Request(null, $params)` never runs core's `absint` `sanitize_callback`, which is the defect). Neither a keyword sweep nor a diff of the assertion finds this one
